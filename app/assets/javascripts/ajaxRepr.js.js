// Generated by CoffeeScript 1.9.1
window.EventTrackerApp = {};

window.EventTrackerApp.ajaxRequests = {
  save: function(url, resource_fields, params) {
    url = url;
    if (params) {
      url += '?' + $.param(params);
    }
    return $.ajax({
      url: url,
      data: resource_fields,
      dataType: 'json',
      type: 'POST'
    });
  },
  del: function(url, resource_id, params) {
    url = url + ("/" + resource_id);
    if (params) {
      url += '?' + $.param(params);
    }
    return $.ajax({
      url: url,
      dataType: 'json',
      type: 'DELETE'
    });
  },
  update: function(url, resource_id, resource_fields, params) {
    url = url + ("/" + resource_id);
    if (params) {
      url += '?' + $.param(params);
    }
    return $.ajax({
      url: url,
      data: resource_fields,
      dataType: 'json',
      type: 'PUT'
    });
  },
  extract: function(url, params) {
    if (params) {
      url += '?' + $.param(params);
    }
    return $.ajax({
      url: url,
      dataType: 'json'
    });
  }
};
