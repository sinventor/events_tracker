// Generated by CoffeeScript 1.9.1
var slice = [].slice;

window.utils = {
  emptyFields: function() {
    var $fields;
    $fields = 1 <= arguments.length ? slice.call(arguments, 0) : [];
    $.each($fields, function(i, field) {
      $(field).empty();
    });
  },
  checkHoursCorrectness: function(hours) {
    var parsedHours;
    parsedHours = parseInt(hours);
    return /\d{2}/.test(hours) && parsedHours >= 0 && parsedHours < 24;
  },
  checkMinutesCorrectness: function(minutes) {
    var parsedMinutes;
    parsedMinutes = parseInt(minutes);
    return /\d{2}/.test(minutes) && parsedMinutes >= 0 && parsedMinutes < 60;
  },
  checkTimeCorrectness: function(time) {
    var hoursAndMinutes;
    hoursAndMinutes = time.split(':');
    if (hoursAndMinutes.length !== 2) {
      return false;
    }
    return this.checkHoursCorrectness(hoursAndMinutes[0]) && this.checkMinutesCorrectness(hoursAndMinutes[1]);
  },
  formatBaseDate: function(date) {
    return moment(date).format('YYYY-MM-DD');
  },
  formatBaseTime: function(date) {
    return moment(date).format('HH:mm');
  }
};
